data:
  raw_dir: "data/raw"
  proc_dir: "data/processed" 
  resources_dir: "resources"

cohort:
  min_los_icu_days: 1.0
  min_duration_hours: 24
  max_duration_hours: 240
  min_age: 15

processing:
  input_window_h: 6
  gap_h: 6
  pred_window_h: 4

artifacts:
  patients_file: "patients.parquet"
  vitals_file: "vitals_labs.parquet"
  vitals_mean_file: "vitals_labs_mean.parquet"
  interventions_file: "interventions.parquet"
  features_file: "dataset_123.parquet"
  labels_file: "labels.parquet"
  output_h5_prefix: "train_coding"
