# configs/config.yaml
defaults:
  - etl: etl_config
  - icd: icd_config
  - cnn: cnn_config
  - _self_

# 실행 모드 (etl, icd, cnn, train, all)
mode: "all"
seed: 42

# 로깅 설정 (Hydra에 의해 보간됨)
logging:
  core_logs_dir: "outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}"
  artifacts_dir: "artifacts"