defaults:
  - _self_
  - etl_config@etl
  - icd_config@icd
  - cnn_config@cnn

# 실행 모드 (etl, icd, cnn, train, all)
mode: "etl"
seed: 42

# 로깅 설정 (Hydra에 의해 보간됨)
logging:
  core_logs_dir: "outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}"
  artifacts_dir: "artifacts"