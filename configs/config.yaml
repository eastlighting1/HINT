defaults:
  - _self_
  - etl_config@etl
  - icd_config@icd
  - cnn_config@cnn

# 실행 모드 (etl, icd, intervention, train, all)
mode: "all"
seed: 42

# 로깅 설정 (Hydra에 의해 보간됨)
logging:
  artifacts_dir: "artifacts"
