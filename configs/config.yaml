defaults:
  - _self_
  - etl_config@etl
  - icd_config@icd
  - intervention_config@intervention

mode: "all"
seed: 42

logging:
  output_dir: "outputs"
  artifacts_dir: "artifacts"
  use_hydra_run_dir: false
